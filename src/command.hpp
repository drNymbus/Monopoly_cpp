#include <iostream>
#include <string>

namespace command {
	unsigned int nb_players(void);
	std::string player_name(void);
	void pick_a_card(void);
	void roll_dice(void);
	void upgrade(void);
}